# Claude Code — מחוז ירושלים

מסמך זה הוא **קובץ אתחול קצר בלבד**. הוא אינו מחליף את חוקת הפרויקט ואינו מקור תוכן עצמאי.

לפני כל שינוי או דיווח:

1. קרא את @RULES.md — מקור הדרישות המחייב.
2. קרא את @README.md — מבנה הפרויקט והפקודות הקנוניות.
3. קרא את @docs/PROJECT_CONTEXT.md — מפת המערכת, נקודות העריכה והסיכונים.
4. אמת הנחות מול הקוד, ה־HEAD הפעיל, `main` וה־CI; מסמכי הקשר אינם תחליף לקריאת הקוד.

## סדר עדיפות במקרה של סתירה

1. הוראה מפורשת ועדכנית של יניב במשימה הנוכחית.
2. `RULES.md`.
3. הקוד והבדיקות ב־HEAD הפעיל וב־`main`.
4. `README.md`.
5. `docs/PROJECT_CONTEXT.md`.

אין לפתור סתירה באמצעות מחיקה שקטה או ניחוש. יש לתעד את הסתירה ולהכריע לפי מקור האמת הגבוה ביותר.

## דרך עבודה מחייבת

- סנכרן לפני שינוי ואל תדרוס עבודה מקבילית. אין לדחוף שינוי ישירות ל־`main`.
- העדף שינוי קטן ומקומי על פני שכתוב רחב.
- תוכן קנוני נערך ב־`src/data/`; אין לשכפל אותו בעמודים או ברכיבים.
- אין להוסיף ספרייה, שירות, API או שכבת מורכבות בלי תועלת מוכחת.
- אין לפרסם קובץ פרטי מ־Google Drive, פרטי גישה, סוד או נתון אישי שאינו מאושר לפרסום.
- אין לטעון שבדיקה, פריסה או קישור הצליחו בלי אימות בפועל.
- cleanup דורש zero-consumer proof; חריגי שימור מפורשים ב־RULES אינם "קוד מת".

## שער איכות לפני מיזוג

```bash
npm ci
npm run quality
```

`npm run quality` הוא מקור האמת היחיד: repo-health → hygiene-dead-code → אימות בניית לוגו Moodle → Astro check → typecheck לבדיקות (`typecheck:tests`) → build → Playwright עם `retries=0`, כולל דסקטופ ו־Pixel 7 לפי חלוקת הפרויקטים.

בנוסף:

- בדוק את ה־diff המלא ואת הקבצים שהשתנו.
- ודא שאין רגרסיה ב־RTL, בנייד, בנגישות, בקישורים, בהטמעות או ב־SEO.
- פתח/עדכן PR עם גבולות השינוי ותוצאות מדויקות.
- מזג רק לאחר CI ירוק; אחרי merge ל־`main`, אימות production חייב להוכיח שה־SHA שנמזג חי.

## תחזוקת הידע

- דרישה חדשה או החלטה מחייבת: `RULES.md`.
- שינוי stack, פקודה או מבנה תיקיות: `README.md`.
- שינוי מפת מערכת, נקודת עריכה או סיכון: `docs/PROJECT_CONTEXT.md`.
- ביקורת/שחזור היסטוריים: `RECOVERY/`.

כל דיווח סיום ליניב יסתיים בקישור החיצוני החי:

https://jerusalem-virid.vercel.app
