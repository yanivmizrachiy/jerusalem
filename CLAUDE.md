# Claude Code — מחוז ירושלים

מסמך זה הוא **קובץ אתחול קצר בלבד**. הוא אינו מחליף את חוקת הפרויקט ואינו מקור תוכן עצמאי.

לפני כל תכנון, שינוי או דיווח:

1. קרא את @RULES.md — זהו מקור הדרישות המחייב.
2. קרא את @README.md — מבנה הפרויקט והפקודות הקנוניות.
3. קרא את @docs/PROJECT_CONTEXT.md — מפת המערכת, נקודות העריכה והסיכונים.
4. אמת את כל ההנחות מול הקוד הפעיל והענף `main`; מסמכי ההקשר אינם תחליף לקריאת הקוד.

## סדר עדיפות במקרה של סתירה

1. הוראה מפורשת ועדכנית של יניב במשימה הנוכחית.
2. `RULES.md`.
3. הקוד הפעיל ב־`main` והבדיקות הקיימות.
4. `README.md`.
5. `docs/PROJECT_CONTEXT.md`.

אין לפתור סתירה באמצעות מחיקה שקטה. יש לעצור, לציין את הסתירה ולהציע הכרעה ממוקדת.

## דרך עבודה מחייבת

- התחל מ־`main` מעודכן וצור ענף ממוקד; אין לדחוף שינוי ישירות ל־`main`.
- לפני קוד, כתוב תוכנית קצרה הכוללת קבצים מושפעים, סיכונים ובדיקות.
- העדף שינוי קטן ומקומי על פני שכתוב רחב.
- תוכן קנוני נערך ב־`src/data/`; אין לשכפל אותו בעמודים או ברכיבים.
- אין להוסיף ספרייה, שירות, API או שכבת מורכבות בלי תועלת מוכחת.
- אין לפרסם קובץ פרטי מ־Google Drive, פרטי גישה, סוד או נתון אישי שאינו כבר מאושר לפרסום.
- אין לטעון שבדיקה, פריסה או קישור הצליחו בלי אימות בפועל.

## שער איכות לפני מיזוג

```bash
npm ci
npm run check
npm run build
npm test
```

בנוסף:

- בדוק את ה־diff המלא ואת הקבצים שהשתנו.
- ודא שאין רגרסיה ב־RTL, בנייד, בנגישות, בקישורים, בהטמעות או ב־SEO.
- פתח PR עם סיכום, גבולות השינוי, סיכונים ותוצאות הבדיקות.
- מזג רק לאחר CI ירוק ובדיקת Vercel מתאימה.

## תחזוקת הידע

- דרישה חדשה או החלטה מחייבת: עדכן את `RULES.md` בסעיף המתאים.
- שינוי ארכיטקטוני או פקודה חדשה: עדכן את `README.md`.
- שינוי במפת המערכת, נקודת עריכה או סיכון: עדכן את `docs/PROJECT_CONTEXT.md`.
- מסמך ביקורת היסטורי או שחזור: שמור תחת `RECOVERY/`.

כל דיווח סיום ליניב יסתיים בקישור החיצוני החי:

https://jerusalem-virid.vercel.app
